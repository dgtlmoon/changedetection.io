# TicketWatch Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# DATABASE CONFIGURATION (Neon.tech PostgreSQL)
# =============================================================================
# Get this from your Neon.tech project dashboard -> Connection Details
# Format: postgresql://username:password@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# =============================================================================
# NOTIFICATIONS
# =============================================================================
# Slack webhook URL for alerts
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# =============================================================================
# PROXY CONFIGURATION (Rotation Middleware)
# =============================================================================
# Proxies can be loaded from files or environment variables.
# Format: protocol://user:pass@host:port or protocol://host:port
# One proxy per line in files, comma or newline separated in env vars.

# General proxy list (file path)
PROXY_LIST_PATH=

# Or provide proxies directly as comma-separated list
# PROXY_LIST=http://proxy1:8080,http://proxy2:8080

# Residential proxy list (optional - for anti-bot escalation)
# RESIDENTIAL_PROXY_PATH=/path/to/residential_proxies.txt
# RESIDENTIAL_PROXIES=http://res1:8080,http://res2:8080

# Datacenter proxy list (optional - faster but more detectable)
# DATACENTER_PROXY_PATH=/path/to/datacenter_proxies.txt
# DATACENTER_PROXIES=http://dc1:8080,http://dc2:8080

# =============================================================================
# SECURITY
# =============================================================================
# Salted password for UI authentication (optional)
# Generate with: python -c "import hashlib; print(hashlib.sha256('yourpassword'.encode()).hexdigest())"
SALTED_PASS=

# =============================================================================
# APPLICATION
# =============================================================================
# Port to run on (default: 5000)
PORT=5000

# Base URL for the application (used in notifications)
BASE_URL=http://localhost:5000
